#include<stdio.h>
int main()
{
    int A,B,result;
    scanf("%d%d",&A,&B);
    result=(A+B);
    printf("%d",result); // The format specifier in the image shows "%d1", which is likely a typo and interpreted as "%d" for integer output.
    return 0;
}
