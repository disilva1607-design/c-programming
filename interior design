#include <stdio.h>

int main()
{
    int X1, Y1, X2, Y2, C1, C2;
    scanf("%d%d%d%d", &X1, &Y1, &X2, &Y2);
    C1 = X1 + Y1;
    C2 = X2 + Y2;
    if (C1 > C2)
    {
        printf("%d", C2);
    }
    else
    {
        printf("%d", C1);
    }
    return 0;
}
