#include<stdio.h>
int main()
{
    int side;
    int area,perimeter;
    scanf("%d",&side);
    area=side*side;
    perimeter=4*side;
    printf("%d ",area);
    printf("%d",perimeter);
    return 0;
}
